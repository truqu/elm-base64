{
    "dependencies": {
        "elm-format": "^0.8.0-alpha-elm019rc1-rc2",
        "elm-test": "^0.18.12",
        "elm-verify-examples": "^2.2.0"
    },
    "scripts": {
        "test": "elm-verify-examples && elm-test",
        "ci": "elm-format --validate src && npm test"
    }
}
