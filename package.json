{
    "dependencies": {
        "elm-format": "^0.8.0",
        "elm-test": "^0.19.0-beta4"
    },
    "scripts": {
        "test": "elm-test",
        "ci": "elm-format --validate src && npm test"
    }
}
